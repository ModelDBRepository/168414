//If Vm traces are desired, run this one.

load_file("CellTemplates.hoc")
load_file("MakeNetwork.hoc")
load_file("RunSim_EP.hoc")

OutputData()

saveSG()
saveT()
saveZ2()
saveEX()

quit()
